import styled from 'styled-components';
import CardGroupConnected from './GroupConnected';

const CardGroup = styled.div`
  display: flex;
`;

CardGroup.Connected = CardGroupConnected;

export default CardGroup;
