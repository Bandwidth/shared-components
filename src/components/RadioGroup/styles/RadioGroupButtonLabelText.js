import styled from 'styled-components';

export default styled.span`
  font-size: inherit;
  font-weight: bold;
  color: inherit;
  margin: auto 0 auto 0;
  line-height: 21px;
  text-align: center;
`;
