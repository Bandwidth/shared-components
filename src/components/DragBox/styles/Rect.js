import styled from 'styled-components';

export default styled.div`
  border-width: 2px;
  border-color: black;
  border-style: dotted;
  z-index: 100000;
  position: fixed;
  pointer-events: none;
`;
