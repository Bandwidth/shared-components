`FormBox` (accessible from `Form.Box`) is the styled border around a standalone form.

```javascript
<Form.Box>
  <Fields columns={1}>
    <Field label="Label:">
      <Input />
    </Field>
  </Fields>
</Form.Box>
```
